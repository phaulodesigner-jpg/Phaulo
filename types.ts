
export interface ExperienceItem {
  year: string;
  role: string;
  company: string;
  description: string;
}

export interface CourseItem {
  provider: string;
  title: string;
}

export interface SkillItem {
  name: string;
  icon: string;
}
